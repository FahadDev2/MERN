import React from 'react';
import { Route, withRouter } from "react-router-dom";

const Home = () => {
    return(
     
      <h1 style={{textAlign: "center", paddingTop: 25, color: "red"}}>Home</h1>

    );
};

export { Home };